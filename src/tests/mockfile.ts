export class AxiosClass {
  get(url: string, formData: {}) {
    //some
  }
  post(url: string, formData: {}) {
    //some
  }
  put(url: string, formData: {}) {
    //some
  }
  delete(url: string, formData: {}) {
    //some
  }
}
